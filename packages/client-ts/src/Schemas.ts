/**
 * Re-export of the model building blocks from `skyportal-js-models`, kept so
 * `import { Schemas } from "skyportal-js"` keeps working.
 *
 * @since 1.0.0
 */

export * from "skyportal-js-models/Schemas";
